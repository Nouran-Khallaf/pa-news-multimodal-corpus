# Runtime manifests

Collection and analysis manifests are written under `outputs/pa_news/manifests/`. This repository directory contains documentation only; do not commit restricted raw captures by default.
